## install
create a new python3 virtualenv
```
python3 -m venv .venv
```
activate it
```
source .venv/bin/activate
```
install the deps
```
pip install -r requirements.txt
```
run the script
```
python ops.py -h
```
